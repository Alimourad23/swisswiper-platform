# CLAUDE.md — SwissWiper Performance Platform

This file tells Claude Code how to build and maintain this project. Read it at the start of every session.

> Tech note: this project uses **Next.js 16** (App Router) — see `AGENTS.md` for the framework's own breaking-change warnings.

## Who you're working with

Ali is a non-technical founder who cannot read code. Explain what you're doing in plain English; give exact terminal commands to copy-paste, one step at a time; never assume knowledge of git, npm, servers, or hosting; confirm before anything destructive; when something breaks, explain the cause simply and propose the fix; prefer the simplest solution that works, no extra dependencies unless needed.

## What we're building

An internal web app — the heart of SwissWiper performance — that the team logs into to see five areas in one place: Emails (Gmail activity), Calendar (agenda, meeting load), Marketing (LinkedIn/Instagram/TikTok), Sales (pipeline, conversion), Orders (order-to-delivery status). A later phase adds JARVIS: a voice layer that speaks a daily briefing and performs a whitelist of safe actions with confirmation.

## Tech stack (keep to this)

Next.js (App Router) + TypeScript; Tailwind CSS; Supabase (Google sign-in) added in the auth phase; hosting on Vercel (auto-deploy from GitHub repo swisswiper-platform); integrations: Google APIs (Gmail, Calendar), Apollo (sales), social platforms later. Do not add other frameworks or large libraries without explaining why and getting Ali's OK.

## Modular architecture (non-negotiable)

Every module is a self-contained vertical slice. Enforce this now and going forward:

- Each module owns three places only: its route (`src/app/dashboard/<module>/`), its logic/data layer (`src/lib/<module>/`), and its components (`src/components/<module>/`).
- Modules are registered in ONE place: `src/lib/modules.tsx` (controls the sidebar + live/coming-soon state).
- **Modules NEVER import from each other.** Anything shared goes through the common foundation: auth, the Supabase clients (`src/lib/supabase/*`), design tokens/primitives (`globals.css`, shared components like `Pill`, `ModuleHeader`), and shared services such as notifications (`src/lib/notifications/`).
- Every external data source sits behind a single function (e.g. `getLinkedInMetrics()`, `getInboxView()`, `getTasksData()`). The UI calls the function; swapping the source (export → API) must not change the UI.
- Each module owns its own database tables with its own RLS. Removing a module must not touch another's data.

## Brand guidelines (non-negotiable — luxury brand)

Colour ratio: White 60% / Black 30% / Periwinkle 10% (accents and CTAs only).

- `--sw-white` #FFFFFF (dominant background)
- `--sw-black` #000000 (text, primary surfaces)
- `--sw-periwinkle` #CAD1E8 (accents, active states, CTAs only)
- `--sw-gray` #EEEEEE (subtle backgrounds, dividers)

Typography: Satoshi (Light/Regular/Medium/Bold) via Fontshare https://api.fontshare.com/v2/css?f[]=satoshi@300,400,500,700&display=swap — no substitute fonts.

Design principles: luxury = subtraction; generous white space; restraint over decoration; no clutter.

Prohibited: chatbots, pop-ups, star ratings, discount language, countdown timers, stock images. Clean, calm, precise — luxury configurator, not SaaS dashboard.

Language: never "buy/order" → use "commission"; never qualify or discount price.

## Build phases (current order)

- **Phase 0 — App shell:** branded layout, sign-in screen (visual placeholder), five module pages as clean placeholders, deployed live to Vercel.
- **Auth** — Real Google sign-in via Supabase + invite-only team allow-list.
- **Phase 1 — Emails module** (Gmail, live). The MVP test.
- **Phase 2 — Calendar module** (Google Calendar, live).
- **Phase 3 — Marketing module** (manual-entry cards first; live later).
- **Phase 4 — Sales module** (Apollo + pipeline; live at sales launch).
- **Phase 5 — Orders module** (schema now; connected at launch).
- **Phase 6 — JARVIS** voice & actions.

Build one phase at a time. Don't scaffold future phases beyond clean placeholders.

## Safety rules

Never commit secrets — keys and Supabase/Google credentials go in `.env.local`, which must be in `.gitignore`; never paste secrets into code or chat. Enforce permissions at the database layer (Supabase row-level security) once auth exists. Keep the main branch deployable; test locally before deploying.

## Working agreement

Start each session by reading this file. Keep a short running progress note at the bottom. Default to small, reviewable changes Ali can see in the browser immediately.

## Local development

- `npm run dev` — start the local site at http://localhost:3000 (this is what Ali opens to look).
- `npm run build` — check the whole site compiles (run before deploying).
- `npm run lint` — check code style. (Next.js 16 removed `next lint`; this calls ESLint directly.)

### Project map (Phase 0)

- `src/app/page.tsx` — sign-in / landing page (placeholder Google button → `/dashboard`).
- `src/app/dashboard/layout.tsx` — dashboard shell (sidebar + top bar + mobile nav).
- `src/app/dashboard/page.tsx` — redirects to `/dashboard/emails`.
- `src/app/dashboard/<module>/page.tsx` — the five module pages.
- `src/lib/modules.tsx` — single source of truth for the five modules (name, slug, subtitle, icon).
- `src/components/` — Sidebar, TopBar, MobileNav, ModulePlaceholder.
- `src/app/globals.css` — brand design tokens (colours + Satoshi font).

### Progress log

- **Phase 0 — app shell built locally (awaiting Ali's visual review).** Next.js 16 (App Router) + TypeScript + Tailwind v4 scaffolded. Brand design system in place: Satoshi from Fontshare; brand colours as CSS variables / Tailwind tokens (`sw-white` / `sw-black` / `sw-periwinkle` / `sw-gray`); light mode only. Sign-in/landing page at `/` (placeholder Google button → `/dashboard`). Dashboard shell at `/dashboard` (left sidebar with 5 modules, active item in periwinkle; top bar with wordmark + placeholder avatar). Five module placeholder pages (Emails, Calendar, Marketing, Sales, Orders) — no fake data. Responsive: sidebar collapses to a horizontal nav on mobile. `npm run build` passes; all routes return 200 locally. Next: after Ali confirms the look, push to GitHub + deploy to Vercel.
- **Phase 0 — re-skinned to "light luxury, elevated" (awaiting Ali's visual review).** New design tokens in `globals.css` (page `#F5F6F9`, surfaces white, ink/muted/hint text, periwinkle system incl. deep `#5C66A6` + soft `#EEF1F8`, hairline border, card shadow, radii). Sidebar now has a "Founder view" section (Overview, Emails·Live, Calendar·Live) and a shaded/dashed "Coming soon" section (Marketing, Sales, Orders, Finance — not clickable). `/dashboard` is now the **Overview** page: greeting hero card + **Alfred** orb (pulsing rings + radial core; tap uses browser `speechSynthesis` to say the greeting and shows a waveform while speaking — placeholder voice, real briefing comes after Emails is live), 4 KPI cards (honest `—` + Soon, no fake numbers), live Email triage + Today panels with "Connect Gmail/Calendar" ready states, and a "Modules connecting soon" row. Module pages restyled to match (live = ModuleHeader + ConnectState; coming-soon = SoonState). The assistant is named **Alfred** everywhere (not JARVIS). New components: `AlfredOrb`, `Pill` (LivePill/SoonPill/ServiceBadge), `ModuleHeader`, `ConnectState`, `SoonState`. All empty states are honest — no fabricated data.
- **Code pushed to GitHub** at `https://github.com/Alimourad23/swisswiper-platform` (branch `main`). Vercel deploy intentionally NOT done yet (pending Ali's local sign-in test).
- **Auth — real Google sign-in via Supabase wired locally (awaiting Ali's test; NOT deployed).** Packages: `@supabase/supabase-js`, `@supabase/ssr`. Keys in `.env.local` (git-ignored): `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`. Clients: `src/lib/supabase/client.ts` (browser), `src/lib/supabase/server.ts` (server, async cookies per Next 16). Route protection uses **`src/proxy.ts`** (Next 16 renamed middleware→proxy) calling `updateSession` in `src/lib/supabase/middleware.ts` — refreshes session + redirects signed-out users away from `/dashboard`. OAuth flow: `GoogleSignInButton` → `signInWithOAuth({provider:'google', redirectTo:/auth/callback})` → `src/app/auth/callback/route.ts` exchanges code → `/dashboard`. TopBar is now an async server component reading the user; `UserMenu` shows real Google name + photo (with initials fallback) and a working **Sign out** button. Overview greeting + Alfred's spoken line use the real first name. Visual design unchanged. NOTE: invite-only team allow-list is still a TODO (any Google account can currently sign in). Supabase Auth must allow-list `http://localhost:3000/**` redirect for local testing.
- **Git author fix (deploy blocker resolved).** Vercel blocked the deploy because commits were authored with a personal Gmail not verified on the GitHub/Vercel account. Set this project's git identity to `Ali Mourad <ali@swisswiper.com>`, re-authored both existing commits with `git filter-branch`, and force-pushed `main`. All commits are now attributed to `ali@swisswiper.com`. Going forward, commits in this repo use that identity automatically.
- **Phase 1 — live Emails module (Gmail read-only) built locally (awaiting Ali's test; NOT deployed).** OAuth now requests `gmail.readonly` + `calendar.readonly` with `access_type=offline` + `prompt=consent` (see `GoogleAuthButton`, replaces old `GoogleSignInButton`). Google **refresh token** captured at `/auth/callback` and stored in Supabase table `public.google_tokens` (RLS: user owns their row) — never exposed to the browser. `src/lib/google/tokens.ts` `getGoogleAccessToken()` reads the refresh token + exchanges it with Google for a fresh access token (needs server-only `GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET` in `.env.local`), with in-memory caching. `src/lib/google/gmail.ts` does READ-ONLY Gmail GETs: `getInboxSummary` (unread / received-this-week / need-attention) and `getTriagedThreads` (sender/subject/date + Priority vs Safe-to-delete tag from Gmail categories + IMPORTANT + no-reply sender signals; "safe to delete" is suggestion-only, nothing is ever modified). `/dashboard/emails` shows counts + triaged list (or a working "Connect Gmail access" state if not yet granted). Overview shows real unread/this-week/attention counts + Unread KPI when connected, else the connect state. REQUIRES (manual, one-time): (1) run the `google_tokens` table SQL in Supabase; (2) paste Google client ID/secret into `.env.local`; (3) sign out & back in to grant the new scopes.
- **Phase 1 counting fix.** Replaced inaccurate Gmail `resultSizeEstimate` (it returned a bogus 201 for both "this week" and "need attention") with real paginated counts (`countMessages`). "Received this week" = actual messages matching `in:inbox newer_than:7d`. "Need attention" = `in:inbox is:unread category:primary` minus no-reply/notification/newsletter senders (small, single-digit). Triage tags now use the same rule: Priority = unread + Primary + real person; Safe = promotions/social/automated; else untagged. Also fixed `gget` to tolerate Gmail's empty response body on zero-result `fields=` queries (was throwing "Unexpected end of JSON input"). Verified against the real inbox: unread=5, week=12, needAttention=0.
- **Emails module polished to match Calendar (local only).** Times now formatted on the CLIENT in the viewer's device timezone with relative labels ("2h ago", "Yesterday") via `EmailsBoard` (client); server `getInboxView` returns raw threads + counts only. Each thread carries Gmail `snippet` (one-line preview), `threadId`, `gmailUrl` (`mail.google.com/.../#inbox/<threadId>` — "Open in Gmail"), and `awaitingReply` (unread + Primary + real person + addressed to me via To header vs Gmail profile email — best-effort). UI: one-line "Inbox load" summary (unread · need attention · safe to delete · received this week), a "Needs your reply" highlight list at top (the awaitingReply set, labelled best-effort), and the triaged list with snippet + relative time + Open-in-Gmail + badge (Awaiting reply / Priority / Safe to delete). Strictly read-only. Key files: `src/lib/google/gmail.ts`, `src/components/email/EmailsBoard.tsx`. Verified: unread=5, needAttention=1, awaitingReply=1, safeToDelete=5, week=12.
- **Phase 3 — Marketing module (LinkedIn live via weekly export; other channels coming-soon, no fake numbers) — local only.** LinkedIn Page analytics come from the user's weekly Excel export (Content/Followers/Visitors `.xls`). Parser `src/lib/linkedin/parse.ts` (uses `xlsx`/SheetJS) detects file type by sheet names and aggregates followers (cumulative + 28d + monthly), impressions (total/28d), engagement (clicks+reactions+comments+reposts → rate), visitors (page views/unique), top posts, and follower demographics. Initial snapshot parsed from Downloads → `src/lib/linkedin/seed-data.json` (re-seed via `node scripts/seed-linkedin.mts`; `scripts/` excluded from tsconfig). Loader `src/lib/linkedin/data.ts` reads latest `linkedin_metrics` Supabase row else the seed. Upload: `/api/linkedin/upload` (server, parses + inserts) + `UploadDropzone` client (the weekly-Friday mechanism). `/dashboard/marketing`: 5 channel cards (LinkedIn LIVE + clickable; Instagram/TikTok/YouTube/Website "Coming soon", NO numbers) + summary KPIs labelled "LinkedIn only". `/dashboard/marketing/linkedin`: KPIs/growth/engagement/visitors/top posts/demographics with blue "Auto (from export)" tags, amber MANUAL "Notable engagers" (`notable_engagers` table, user-added), and the upload drop-zone. Marketing moved to founder nav (clickable, active on sub-pages). REQUIRES (one-time SQL): `linkedin_metrics` + `notable_engagers` tables (RLS per user; `notable_engagers.user_id default auth.uid()`). Dashboard shows real seed numbers even before the SQL. Parsed real data: followers 38, impressions 1,918, engagement 10.43%, visitors 544 views/249 unique.
- **Phase 3+ — Marketing/LinkedIn analytics upgrade (local only).** Parser now stores DAILY series (`daily.content` with organic/sponsored kept separate, `daily.followers`, `daily.visitors`) + all posts (incl CTR + content type) + demographics, so any window recomputes client-side. `src/lib/linkedin/compute.ts` (pure) does windowed aggregates + previous-equal-window, decision-maker share, content-type breakdown, best-by-CTR, awareness→inquiry funnel, and the Alfred insight. UI: **7/30/365-day time toggle** (default 365) on both Marketing overview and LinkedIn pages (client components `MarketingOverviewClient`, `LinkedInClient`, `TimeToggle`); every KPI shows **period-over-period** (`Delta`); **Awareness→Inquiry funnel** (impressions→clicks(CTR)→page visits→inquiries) where inquiries is a manual number stored in `marketing_inputs` (one-time SQL); **Decision-maker share** headline (director+ % of followers) with a ring; **content-type performance** (video vs text/image avg engagement + best-by-CTR); **Alfred insight line**. Re-seed via `node scripts/seed-linkedin.mts`. Verified per window — 7d: imp 174/clicks 12 (CTR 6.9%)/eng 9.8%; 30d: imp 1,918/clicks 168 (8.76%)/eng 10.4%/+37 followers; 365d: same + 38 followers; DM share 33.3%; Video 14% eng vs Text 7.6%.
- **Phase 1 consistency fix.** "Need attention" and the "Priority" tag previously used two different mechanisms (a `category:primary` search query vs. label logic) and could disagree (a Priority-tagged email counted 0). Unified into a single `classify()` function in `gmail.ts`: an email is "priority"/"needs attention" iff unread + Primary (CATEGORY_PERSONAL or no non-primary category) + real person (not promotions/social/automated sender). New `getInboxView(token)` fetches ONE window of recent inbox messages (18), classifies each once, builds the triaged list, and sets `needAttention = count of priority items in that same set` — so count and tags can never disagree (no window mismatch). Both `/dashboard/emails` and Overview use `getInboxView`. Verified: needAttention=1, priorityInList=1, match=true.
- **Phase 2 — live Calendar module (Google Calendar read-only) built locally (awaiting Ali's test; NOT deployed).** Reuses the existing token/refresh setup (`calendar.readonly` already granted). `src/lib/google/calendar.ts` `getCalendarView(token)`: READ-ONLY events.list on `primary` calendar (`singleEvents=true`, `orderBy=startTime`), detects the calendar's own timezone (`/calendars/primary` → `timeZone`), groups events by local date for today + next 6 days (7-day window), excludes cancelled/declined. Returns `{timeZone, meetingsToday, meetingsWeek, todayEvents, days}`. Overview: "Today" panel shows today's real agenda (start time + title) or "Nothing scheduled today"; "Meetings today" KPI shows real count (Soon tag removed when connected). `/dashboard/calendar`: summary (meetings today / this week) + Next-7-days grouped by day. Verified against real calendar: tz=Africa/Cairo, meetingsToday=3, meetingsWeek=24.
- **Phase 2 — Calendar built out to a full polished view (local only).** Timezone now follows the VIEWER's device: server (`getCalendarData`) returns raw events + tz-independent facts only; all "today"/"this week"/time formatting happens client-side using native `Date` (device tz), with a "Times shown in {tz}" label. Meeting vs reminder: `isMeeting` = has another attendee OR a video link; solo events are reminders (shown with a "Reminder" tag, excluded from meeting counts). Each meeting shows a one-line purpose (description, else attendees/location). Features in `CalendarBoard` (client): "Up next" with live countdown + Join button (Meet/Zoom/Teams via hangoutLink/conferenceData/url scan), red "Overlaps" badge on clashing meetings (overlap computed server-side on instants), today's load (meetings · hours booked · longest free block in 8:00–20:00), pending-invites nudge (display-only, never accepts/declines), Tomorrow glance, and Next-7-days grouped. Overview uses client islands `OverviewToday` + `MeetingsTodayKpi`. Key files: `src/lib/google/calendar.ts` (server fetch), `src/lib/calendar-view.ts` (pure client helpers), `src/components/calendar/*`. Strictly read-only. Verified: tz=Africa/Cairo, meetingsToday=0, remindersToday=3, meetingsThisWeek=1, conflicts=0, pendingInvites=0 (today's events are solo reminders, not meetings).
- **Alfred/dashboard fixes: Julian in overlay, compact summon panel, perf — local only; NOT deployed.** (1) **Julian voice in the overlay**: `voice.ts` now plays through ONE persistent `<audio>` element and exports `unlockAudio()` (plays a programmatic 0.1s silent WAV during a gesture to unlock playback) — called on every summon gesture (star button, Ctrl/⌘+. hotkey, wake toggle, mic tap, Bridge first-gesture). This stops ElevenLabs from being blocked mid-page and silently falling back; browser `speechSynthesis` is only used on a genuine API error. (2) **Compact summon panel**: `AlfredOverlay` is no longer a full-screen `inset-0` blurred takeover — it's a small bottom-centered deep-space card (`pointer-events-none` wrapper so the page behind stays visible + usable), with a small star + voice, dismissible by Esc / close / "that's all". The full-screen star remains only the Bridge welcome. (3) **Performance**: the heavy module pages (overview, emails, calendar, marketing) now render the shell instantly and **stream the slow Gmail/Calendar/LinkedIn/Tasks data via `<Suspense>`** (overview also parallelizes its fetches) instead of blocking navigation; and the star + starfield canvases **pause their rAF loop when the tab is hidden or the canvas is off-screen** (visibilitychange + IntersectionObserver). `npm run build` passes.
- **Bridge briefing — full only on first sign-in/after idle, else a short greeting — local only; NOT deployed.** `composeBriefing(data, now, mode)` now takes a `full`/`short` mode. The Bridge decides per device (localStorage `sw-bridge-briefed-at` + sessionStorage `sw-bridge-session`): **full** on the first Bridge visit of a session OR when >3h since last OR a new calendar day; **short** otherwise. Full opens with Alfred introducing himself, time-aware ("Good morning, Ali. This is Alfred, your personal assistant on the SwissWiper platform. Here's where things stand…") then the spoken status lines (and the on-screen synthesis + lines). Short shows/speaks only a time-aware greeting + "How may I be of assistance?" with NO status lines and no long read. After being idle/away (>3h or next day) the full briefing returns. Star + voice aesthetic unchanged. `npm run build` passes.
- **Alfred — continuous hands-free listening + free-form instruction understanding — local only; NOT deployed.** (1) **Continuous conversation**: once you start (tap mic / wake word), the mic keeps listening across turns — Alfred auto-resumes after each reply (and through silent pauses) until you say **"that's all"** (or dismiss / Esc). A subtle status under the star reads "Listening — say 'that's all' when you're done." Tapping the mic pauses it. Implemented as a resume-after-speech loop with `stoppingRef` to suppress auto-continue around intentional stops, and "latest function" refs so a recognizer created in an earlier render never sends stale conversation history. (2) **Free-form understanding**: while a review panel is open, only short/clear words map to buttons (send/save/create/confirm/cancel, bare revise→"what to change?"); anything else (e.g. "make it friendlier", "mention his holiday", "make it shorter", "change the due date to Monday", "add Etienne too") is sent to the brain with the current draft embedded, so Alfred **applies the edit and re-presents the updated review in place** with a natural acknowledgement — no need to say "redraft" first. Unrelated questions are answered with the panel left open. Buttons remain the reliable fallback; the system prompt now tells Alfred to re-call the same tool on plain-language edits and acknowledge briefly. `npm run build` passes.
- **Alfred — one consistent review-panel pattern for ALL actions — local only; NOT deployed.** Every proposal now renders through a shared `ActionPanel` (`src/components/bridge/ActionPanel.tsx`): a wide (`max-w-2xl`), polished deep-space card with title, the editable fields, a standardized row of **action options as buttons**, and a "Say:" hint. The three review components (`TaskReview`/`EmailReview`/`EventReview`) are now **fields-only**; AlfredChat owns the options per kind. Options are available as **buttons AND voice**, and Alfred **speaks the choices** when a panel opens — EMAIL: Send · Redraft · Save draft · Cancel; TASK / EVENT: Create(or Reschedule) · Revise · Cancel; STATUS/cancel-event/simple confirms: Confirm · Cancel (cancel-event uses a red "danger" confirm). A general **Revise** intent ("change it"/"redo"/"not quite"/"redraft"/"rewrite") on any task/email/event panel re-opens the conversation ("what would you like to change?"), sends the current draft + the requested change to the brain, and re-presents the **updated review in place** (applied atomically so the greeting doesn't flash). Voice maps: send/send-it→Send, draft/save→Save draft, create/yes/confirm→Create/Confirm, cancel/no→Cancel. While any panel is open, the Bridge **hides the greeting + briefing + "Enter dashboard"** and the overlay hides its label/dismiss hint, and the **star shrinks** — full focus, one viewport, no scroll; restored on close (AlfredChat signals via new `onPanelOpenChange`). Pure star + voice aesthetic kept. `npm run build` passes.
- **Build B — Alfred ACTIONS: Email + Calendar, and Tasks fix — local only; NOT deployed.** Extends the whitelisted tool-use (propose→review→confirm→execute; nothing changes data without explicit user confirmation). (A) **Tasks fix**: `/api/alfred/chat` now ALWAYS returns the resolution `directory` (was gated to task actions only — that's why name/title resolution could be empty in the summon overlay), and the system prompt makes tool use explicit. create_task (editable review) + set_task_status (confirm) work on Bridge and overlay. (B) **Email (Gmail, DRAFT-FIRST)**: tools `draft_reply`/`draft_email` → editable `EmailReview` → on confirm a server action (`src/lib/google/gmail-actions.ts`, uses `getGoogleAccessToken`) creates a Gmail **draft** (replies thread via `threadId` + `In-Reply-To`/`References`); Alfred says "I've drafted it in your Gmail" — never sends. **Sending is separate + explicit**: `send_email` only when the user clearly says "send", with its own Send-styled confirm. (C) **Calendar (`calendar.events`)**: tools `create_event`/`reschedule_event`/`cancel_event` → editable `EventReview` (cancel uses a clear confirm) → server actions (`src/lib/google/calendar-actions.ts`) insert/patch/delete on the primary calendar (local datetime + IANA `timeZone`). People/emails/events matched against a real directory (teammates, open tasks, recent emails incl. `senderEmail`, upcoming events); unresolved → Alfred asks. All Google tokens stay server-side. Pure star + voice UI kept (review panels are the only on-screen elements while pending). `npm run build` passes.
- **Google OAuth — write scopes + incremental auth (no more per-login consent) — local only; NOT deployed.** `GoogleAuthButton` now requests write scopes alongside the existing read ones (added `gmail.compose` + `calendar.events`; kept `gmail.readonly` + `calendar.readonly`) so Alfred can later draft email / manage events (actions NOT built yet — scopes only). Removed the forced `prompt=consent` from normal sign-in; kept `access_type=offline` and added `include_granted_scopes=true` (incremental auth) → the new scopes trigger **one** consent on the next login, then logins are seamless. `/auth/callback` already only upserts `google_tokens.refresh_token` when `provider_refresh_token` is present — made that intent explicit (seamless logins return no refresh token; we must NEVER overwrite the stored one with null, or Gmail/Calendar breaks). Added a small **"Reconnect Google"** action in `UserMenu` (new `GoogleAuthButton` `variant="subtle"` + `forceConsent`) that forces the consent screen to recover a lost token / re-grant scopes. `npm run build` passes.
- **Phase 6 — Alfred's premium voice (ElevenLabs "Julian") — local only; NOT deployed.** Replaced browser `speechSynthesis` with the ElevenLabs voice **Julian** (`7p1Ofvcwsv7UBPoFNcpI`) for BOTH the Bridge briefing and every conversation reply. New server route **`/api/alfred/speak`** ({text} → mp3) calls ElevenLabs TTS (`eleven_turbo_v2_5`, voice settings stability 0.45 / similarity 0.75 / style 0.15 / speaker-boost) with `process.env.ELEVENLABS_API_KEY` (server-only; requires sign-in so the key can't be abused); it **streams** the mp3 through. Shared `src/lib/bridge/voice.ts` `speak()` now POSTs to that route and plays the audio via an `HTMLAudioElement`, tying the star's speaking flare to play→ended; added `stopSpeaking()` (used by AlfredChat to interrupt + on overlay close) and a generation counter so a stale TTS fetch never plays over a newer one. **Graceful fallback**: on API error / missing key it falls back to browser `speechSynthesis` (`browserSpeak`) so Alfred is never silent; autoplay-blocked cold loads defer to the existing first-gesture retry (Bridge skips the initial fetch unless `navigator.userActivation.hasBeenActive`, to avoid wasting a TTS call). Brain/actions/overlay/wake word unchanged. REQUIRES one-time: add `ELEVENLABS_API_KEY` to `.env.local` (and Vercel). `npm run build` passes.
- **Phase 6 — Alfred everywhere: summon from any dashboard page — local only; NOT deployed.** Alfred is now reachable on every dashboard page (fixes "couldn't talk to him after he moved me"). New `AlfredSummon` (in `src/app/dashboard/layout.tsx`, so it + its conversation persist across in-dashboard navigation) provides three summon paths: a small understated **star button** (fixed bottom-right, always works), a **hotkey** (Ctrl/⌘ + .), and a **"Hey Alfred" wake word** — a background `SpeechRecognition` behind a toggle that's **OFF by default** (persisted `sw-alfred-wake`; always-on mic has battery/privacy cost) and pauses while the overlay is open. Summoning shows `AlfredOverlay`: the same star + voice conversation over a dimmed deep-space backdrop, dismissible by Esc / tap-outside / the close control / saying **"that's all"**. **Continuity**: `AlfredChat` stays mounted across open/close (heavy canvases mount only while open) so the conversation + actions persist; after a `navigate` action the overlay stays open so the user keeps talking, while the post-create "open it" offer closes the overlay to reveal the task. Shared `AlfredStar`/`AlfredChat`/`voice` are reused (AlfredStar gained a `className` size prop; AlfredChat gained `active`/`onDismiss`/`autoListenKey` props — Bridge unchanged via defaults). Bridge remains the full-screen welcome. `npm run build` passes.
- **Phase 6 — Alfred actions polish: multiple assignees + editable task review — local only; NOT deployed.** (1) **Multiple assignees**: QuickAdd/TaskDetail already multi-select via `AssigneeSelect` (no change); Alfred's `create_task` tool now takes **`assigneeNames[]`**, each matched to a real profile (first/full/single-substring) — unresolved names are surfaced, never invented. (2) **Create = review before create**: the small Confirm/Cancel card for create is replaced by an **editable, prefilled task review** on the Bridge (`src/components/bridge/TaskReview.tsx`, dark/on-brand) — title, multi-assignee chips, due date, priority, visibility (Founders shown only if `directory.userRole==='founder'`), every field editable. Alfred speaks a deterministic **summary** of what he drafted; the user reviews/edits then **Create** (button or voice "yes") — only then `createTask` runs. After creating, Alfred confirms and **offers to open it** (reuses the simple confirm → `router.push('/dashboard/tasks?task=<id>')`). `set_task_status` keeps its Confirm/Cancel card; navigation unchanged. Pure star + voice aesthetic kept (review panel is the only on-screen element while pending). `npm run build` passes.
- **Phase 6 — Alfred Step 2b: ACTIONS (whitelisted tool-use) — local only; NOT deployed.** `/api/alfred/chat` now defines a small **Anthropic tool-use** whitelist (model still `claude-sonnet-4-6`); the route returns the proposed tool call(s) + a resolution **directory** (real teammates + open tasks) and the **client executes**. Two safety tiers: (1) **navigate**(`overview|tasks|calendar|emails|marketing|bridge`) — safe + immediate, client `router.push`es and Alfred gives a short spoken confirmation; (2) **create_task** / **set_task_status** — data changes that **require explicit confirmation** first: an on-brand confirm card (Confirm/Cancel) that's **also answerable by voice "yes"/"no"** (auto-listens after the proposal), and only on confirm does the client call the existing server actions `createTask`/`setStatus`. Never executes a data change without confirmation; assignee/task names are matched against the real directory (first/full name, single substring) — unresolved → Alfred asks, never invents. System prompt updated: **LinkedIn figures ARE the marketing data** ("how's marketing?" answered from LinkedIn), the marketing line loads reliably each turn (seed fallback), and Alfred is told navigation is immediate while task actions are proposals. Email/calendar actions are intentionally NOT included (need extra Google scopes). Pure star + voice UI kept (no transcript). `npm run build` passes.
- **Phase 6 — Alfred Step 2: conversation layer (talk to Alfred, he answers in voice) — local only; NOT deployed.** Push-to-talk on the Bridge: a mic control (`AlfredChat`, `src/components/bridge/`) uses the browser **SpeechRecognition** API to transcribe speech, posts the conversation to **`/api/alfred/chat`**, then speaks the reply via `speechSynthesis` — the raging star intensifies while Alfred speaks (shared `speaking` state) and calms when done. The route (`src/app/api/alfred/chat/route.ts`, server-only) calls the **Anthropic API** (`@anthropic-ai/sdk`, model `claude-sonnet-4-6`) with `process.env.ANTHROPIC_API_KEY` (never exposed to the browser). Alfred's persona = a refined, calm, dry-witted British butler (Alfred Pennyworth) who addresses the user by first name; each turn injects a **compact READ-ONLY summary** of the same live data the Bridge uses (tasks open/overdue/due-today, today's calendar + next meeting, inbox unread/priority/awaiting-reply, LinkedIn pulse — all tz-aware via the client's IANA timezone) so he can answer "what's overdue?", "what's my day?", "summarise my inbox", "how's LinkedIn?". He advises only — no actions yet. Minimal on-brand transcript (You / Alfred) beneath the star; the static briefing lines hide once a conversation starts to keep the single viewport; graceful quiet errors (no-mic, blocked permission, API failure) — never crashes. Shared voice helper `src/lib/bridge/voice.ts` (`pickVoice` prefers en-GB + `speak`). REQUIRES one-time: add `ANTHROPIC_API_KEY` to `.env.local` (and Vercel). `npm run build` passes.
- **Round 2 — Tasks: founders-only visibility + day-before reminder (local only; awaiting Ali's test + one-time SQL; NOT deployed).** (1) `profiles.role` (`member`/`founder`, default `member`, checked) gates a new task visibility value `founders` (alongside `team`/`personal`). RLS: founders-visibility tasks are SELECT/UPDATE-able only by `role='founder'` (non-recursive `exists` against `profiles`, whose own SELECT policy is `true`); team/personal keep their creator/assignee rules and founders rows are walled off even from a member creator/assignee. The auth-callback profile upsert deliberately omits `role`, so it's set to the default only on first sign-in and preserved on later logins (founders never reset to member). UI: a shared `VisibilitySelect` (Team/Personal/Founders) in `QuickAdd` + `TaskDetail` — the **Founders** option shows only when the signed-in user is a founder (role threaded server→client via `getTasksData().userRole`); founders tasks get a distinct deep-periwinkle badge. (2) Daily reminders cron now also includes tasks **due tomorrow** (reusing the `due` notification type with a "Due tomorrow: …" message + a "Due tomorrow — <title>" digest line), alongside overdue + due-today, still one grouped email per person. REQUIRES one-time SQL: `supabase/tasks-founders-migration.sql` (adds `profiles.role`, widens the `tasks.visibility` check, updates `tasks_select`/`tasks_update`), then `update public.profiles set role='founder' where email=…` for each founder. `npm run build` passes.
- **Phase 3.5 — Tasks module (shared team to-do) + reusable notification service (local only; awaiting Ali's test + one-time SQL; NOT deployed).** Established the **modular architecture** rules (see section above) and added them to this file. New shared foundation: `src/lib/supabase/admin.ts` (service-role client, server-only — used only by notifications + cron; no-ops gracefully if `SUPABASE_SERVICE_ROLE_KEY` unset) and `src/lib/notifications/` (`notify({userId,actorId,type,taskId,message})` writes an in-app row via the admin client and, for `assigned`/`mentioned`, also sends an instant email; `sendEmail` posts to Resend's REST API — no SDK dependency — and no-ops if `RESEND_API_KEY` unset; on-brand plain `emailTemplate`). Tasks module is a clean slice: `src/lib/tasks/{types,data,actions,format}.ts` (data behind `getTasksData()`; server actions `createTask/updateTask/setStatus/addAssignee/removeAssignee/deleteTask` + `@mention` parsing that only pings newly-added names) and `src/components/tasks/*` (`TasksBoard` — list/board toggle, drag-between-columns to change status, Mine/All/Overdue/Due-this-week + tag + assignee filters, sort by due/priority, Supabase **Realtime** subscription on `tasks`+`task_assignees` → debounced `router.refresh()` so the team sees changes live; `QuickAdd`, `TaskDetail` slide-over, `Avatar`/`AvatarStack`, `AssigneeSelect`, `TasksPulse`). Due dates render CLIENT-side in the viewer's tz with relative labels + red when overdue (`format.ts`). Page `src/app/dashboard/tasks/page.tsx` (`?view=mine` → My tasks; `?view=board` → Team board; `?task=<id>` opens detail). **Notification bell** added to `TopBar` (`NotificationBell`, Realtime-filtered to the user, unread badge, dropdown, click→open task + mark read, mark-all-read). **Cron**: `src/app/api/tasks/reminders/route.ts` (GET, `Authorization: Bearer $CRON_SECRET`) groups due-today/overdue not-done tasks per assignee, writes `due`/`overdue` notifications, sends ONE digest email each; `vercel.json` runs it `0 6 * * *`. Auth callback now upserts a `profiles` row (name/avatar/email) on every sign-in. Tasks added to founder nav as LIVE with children (My tasks / Team board) + to `modules`. Overview gained a "Tasks pulse" panel (my open / due today / overdue, honest empty state). REQUIRES (one-time): run the Tasks SQL (profiles, tasks, task_assignees, notifications + RLS + realtime publication); add env vars `SUPABASE_SERVICE_ROLE_KEY` (in-app notifications + cron), `RESEND_API_KEY` (emails), `CRON_SECRET` (protect the cron) to `.env.local` and Vercel; optional `NEXT_PUBLIC_APP_URL` for email links. `npm run build` passes.
